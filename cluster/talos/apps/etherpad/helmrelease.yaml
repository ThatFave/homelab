apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: etherpad
  namespace: etherpad
spec:
  interval: 5m
  chart:
    spec:
      chart: etherpad
      version: "0.0.8"
      sourceRef:
        kind: HelmRepository
        name: redhat-cop
        namespace: flux-system
  values:
