apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: davmail
  namespace: davmail
spec:
  interval: 5m
  chart:
    spec:
      chart: davmail
      version: ">=0.1.0"
      sourceRef:
        kind: HelmRepository
        name: davmail
        namespace: flux-system
